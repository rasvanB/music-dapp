import { NextPage } from "next";
import UserProfile from "../../components/user-profile";

const Home: NextPage = () => {
  return (
    <div>
      <UserProfile />
    </div>
  );
};

export default Home;
